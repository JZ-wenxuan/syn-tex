{
  "name": "syn-tex",
  "scripts": {
    "start": "parcel serve ./src/index.html",
    "build": "rm -rf dist && parcel build ./src/index.html --public-url \"https://ayassaka.github.io/syn-tex/\"",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "brace": "^0.11.1",
    "gh-pages": "^3.2.0",
    "mathjax-full": "file:../MathJax-src"
  },
  "devDependencies": {
    "parcel": "next"
  }
}
